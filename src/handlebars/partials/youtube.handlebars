<iframe
  class="pageContent_youtube"
  {{#if width}}
    width="{{width}}"
  {{else}}
    width="560"
  {{/if}}
  {{#if height}}
    height="{{height}}"
  {{else}}
    height="315"
  {{/if}}
  src="{{src}}"
  frameborder="0"
  allowfullscreen>
</iframe>
